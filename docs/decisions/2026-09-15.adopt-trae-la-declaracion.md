# adopt-trae-la-declaracion — `adopt` y `pull` traen el bilink entero, no sólo su `accepted`

**Estado:** En desarrollo · **Versión:** 1.4 · **Última actualización:** 2026-09-15

**Dónde cae.** Solo en bilinker. Toca la spec de la ref, en `docs/specs/concepts/ref.md`, § "`bilinker adopt`", § "La base del merge a tres puntas sale gratis" y § "Qué es conflicto en `pull` y qué no" y § "`agree` sólo se agrega a sí mismo".

---

## Contexto

`adopt` y `pull` comparten un merge a tres puntas que compara, por endpoint, los campos de `accepted`, y nada más. El formato le da a cada endpoint dos ejes con dos escritores: `apply` escribe la declaración —`link` y `n.1.link`— y `accept` la decisión. Una decisión se toma siempre sobre una declaración, y el merge trae una sin la otra.

Medido el 2026-09-15 al llevar a `main` la rama `acc-363` de lspd, que salía del `main` de la ref sin que `main` hubiera tocado nada: `adopt acc-363` trajo 11 campos de `accepted` y dejó afuera 16 bilinks creados en la rama, 30 captures y los `link` y `n` repuntados con `apply` y `accept --place`. El endpoint `e9f53837.1` quedó con la decisión de `slot_for` sobre la declaración de `client_for`, `RELOCATED`, y con un `accepted.link` a un capture que la ref no tiene. En bilinker, la misma rama tenía 6 bilinks nuevos, 28 captures y 57 bilinks modificados.

La salida que quedaba era mover `refs/bilink/main` con git a mano, que es lo que la ref prohíbe.

---

## Decisión

### 1. La declaración se compara igual que la decisión

`link` y `n` de cada endpoint entran al merge a tres puntas con las mismas filas que `accepted`: entra limpio si sólo el vecino lo cambió, ya coincidía si los dos escribieron lo mismo, conflicto si los dos lo cambiaron a valores distintos, y nada si sólo cambió acá.

Se descarta traer la declaración sólo cuando viaja su `accepted`. `apply` repunta sin aceptar, y esa declaración del vecino es tan cierta como la otra: dejarla afuera es la misma pérdida con otra condición.

Un conflicto de declaración no se une, a diferencia de `accepted`: un endpoint tiene un solo `link`. Frena el comando como cualquier conflicto, y se resuelve con `apply` o `recapture` de un lado.

**Avance: 1/1.**

| Dimensión | Estado | Tarea | Evidencia |
|---|---|---|---|
| `link` y `n` en el merge a tres puntas | `cerrada` | `@bilinker-adopt-trae-las-aceptaciones-del-vecino-sin-sus-bilinks-nuevos-sus-captures-ni-sus-declaraciones` | rama `adopt-incompleto`, `DECLARATION` en `adopt.rs` ↔ esta sección, por la spec |

### 2. Un bilink que sólo tiene el vecino entra entero

Un bilink que no está en la base ni acá, y sí en el vecino, entra con el archivo del vecino. Es la fila "entra limpio" aplicada al archivo en vez de al campo: nadie de este lado decidió nada sobre él.

**Avance: 1/1.**

| Dimensión | Estado | Tarea | Evidencia |
|---|---|---|---|
| Bilinks nuevos del vecino | `cerrada` | `@bilinker-adopt-trae-las-aceptaciones-del-vecino-sin-sus-bilinks-nuevos-sus-captures-ni-sus-declaraciones` | rama `adopt-incompleto`, `diff3` en `adopt.rs` ↔ esta sección, por la spec |

### 3. Los captures entran por unión

Un capture es inmutable y su nombre es el hash de su ubicación, así que dos captures con el mismo nombre son el mismo archivo. Todo capture del vecino que no está acá entra, y nunca hay conflicto. Uno que no referencia nadie lo saca `capture prune`, como siempre.

**Avance: 1/1.**

| Dimensión | Estado | Tarea | Evidencia |
|---|---|---|---|
| Captures del vecino por unión | `cerrada` | `@bilinker-adopt-trae-las-aceptaciones-del-vecino-sin-sus-bilinks-nuevos-sus-captures-ni-sus-declaraciones` | rama `adopt-incompleto`, `apply_changes` en `adopt.rs` ↔ esta sección, por la spec |

### 4. `adopt` no borra

Un bilink que está en la base y no en el vecino se reporta y se queda. Borrar un bilink es su última aceptación como rastro, y `adopt` no decide nada. Quien lo quiere afuera corre `bilinker remove`.

**Avance: 1/1.**

| Dimensión | Estado | Tarea | Evidencia |
|---|---|---|---|
| Un borrado del vecino se reporta y no se aplica | `cerrada` | `@bilinker-adopt-trae-las-aceptaciones-del-vecino-sin-sus-bilinks-nuevos-sus-captures-ni-sus-declaraciones` | rama `adopt-incompleto`, `diff3` en `adopt.rs` ↔ esta sección, por la spec |

### 5. La base del merge es el ancestro común más nuevo que es de la ref

`git merge-base` entre dos tips de la ref devuelve el ancestro común más nuevo, y después de un `merge --no-ff` en el proyecto ése es un commit **del proyecto**: la rama absorbió su tip, y `main` absorbió el merge que lo contiene. Un commit del proyecto no tiene `.bilink/`, así que la base queda vacía y todo campo que difiere es conflicto.

Medido el 2026-09-15 en bilinker: después de `merge --no-ff` de `adopt-incompleto` y de su `adopt`, `adopt acc-363` tomó como base `bb7d974`, del proyecto, y dio 57 conflictos donde no había ninguno.

La base es el ancestro común más nuevo que lleva `.bilink/` en su árbol. Es el mismo freno que ya usan la absorción y el registro de decisiones para no salir de la ref.

**Avance: 1/1.**

| Dimensión | Estado | Tarea | Evidencia |
|---|---|---|---|
| La base del merge, restringida a la ref | `cerrada` | `@bilinker-adopt-trae-las-aceptaciones-del-vecino-sin-sus-bilinks-nuevos-sus-captures-ni-sus-declaraciones` | rama `adopt-incompleto`, `ref_merge_base` en `bilink_ref.rs` ↔ esta sección, por la spec |

### 6. Una sincronización trae aprobaciones, no las agrega

`verify-ref` exige que los nombres que un commit agrega a `agree` sean su autor, y compara contra el primer padre. Un commit de `adopt` o de `pull` tiene dos padres de la ref, y los nombres que trae del segundo ya estaban aprobados ahí, en commits que se verificaron con su propio autor. Comparados sólo contra el primero, parecen agregados por quien sincroniza.

Medido el 2026-09-15 en lspd: el commit de `adopt acc-363`, de `anibal`, quedó rechazado por traer 41 `agree` de `Anibal`, el autor de la rama.

En una sincronización, un nombre está agregado sólo si no está en ninguno de los dos padres para ese endpoint. Lo que trae el segundo padre no es una atestación nueva.

**Avance: 1/1.**
| Dimensión | Estado | Tarea | Evidencia |
|---|---|---|---|
| `verify-ref` compara el `agree` de una sincronización contra los dos padres | `cerrada` | `@bilinker-adopt-trae-las-aceptaciones-del-vecino-sin-sus-bilinks-nuevos-sus-captures-ni-sus-declaraciones` | rama `adopt-agree`, `verify_agree` en `verify.rs` ↔ esta sección, por la spec |

---

## Lo que esta decisión no decide

- **Unir un `accepted` en conflicto en vez de frenar.** La spec ya lo dice, en § "La divergencia se une, y deja de bloquear", y el código todavía frena. Es otra tarea.
- **Que `remove` publique lo que borra.** Es otra tarea, y no cambia que `adopt` no borre.
- **`kind`, `name` y `as`.** Son inertes; viajan con un bilink nuevo, y sobre uno que ya existe no se comparan.

---

## Historial

| Versión | Fecha | Qué cambió |
|---|---|---|
| 1.4 | 2026-09-15 | La sección 6 se cierra, con código, spec y bilinks aceptados. |
| 1.3 | 2026-09-15 | Sección 6: `verify-ref` rechazaba los `agree` que una sincronización trae del otro padre. |
| 1.2 | 2026-09-15 | La sección 5 se cierra, con código, spec y bilinks aceptados. |
| 1.1 | 2026-09-15 | Sección 5: la base del merge sale de la ref, porque después de un `merge --no-ff` `git merge-base` devuelve un commit del proyecto. |
| 1.0 | 2026-09-15 | Pasa a en desarrollo: las cuatro secciones cerradas, con código, spec y bilinks aceptados en la rama `adopt-incompleto`. |
| 0.1 | 2026-09-15 | Nace de llevar `acc-363` de lspd y bilinker a `main`: `adopt` trajo las aceptaciones sin los bilinks, captures y declaraciones que las sostienen. |
